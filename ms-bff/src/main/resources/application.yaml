spring:
  application:
    name: ms-bff

  cloud:
    openfeign:
      client:
        config:

          default:
            connectTimeout: 3000
            readTimeout: 5000

          ms-login:
            url: http://localhost:8081

          ms-inventory:
            url: http://localhost:8082

server:
  port: 8080