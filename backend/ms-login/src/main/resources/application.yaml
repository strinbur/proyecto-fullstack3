spring:
  application:
    name: ms-login

  mongodb:
    uri: mongodb://mongodb:27017/login_bd

jwt:
  secret:
    key: "f9A2kL7pQzR4vX9mN3bH5jW8sT1yC6xD"
  expiration: 86400000

mongock:
  migration-scan-package:
    - com.grupocordillera.ms_login.migrations

server:
  port: 8081

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

sentry:
  dsn: ${SENTRY_DSN}
  environment: dev
  traces-sample-rate: 1.0