apiVersion: v1
kind: Service
metadata:
  name: ms-login
  labels:
    app: ms-login
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: ms-login
