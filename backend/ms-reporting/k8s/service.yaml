apiVersion: v1
kind: Service
metadata:
  name: ms-reporting
  labels:
    app: ms-reporting
spec:
  type: ClusterIP
  ports:
  - port: 8087
    targetPort: 8087
    protocol: TCP
  selector:
    app: ms-reporting