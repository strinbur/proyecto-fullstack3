apiVersion: v1
kind: Service
metadata:
  name: ms-order
  labels:
    app: ms-order
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: ms-order
