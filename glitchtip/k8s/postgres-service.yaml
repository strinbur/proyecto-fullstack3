apiVersion: v1
kind: Service
metadata:
  name: glitchtip-postgres
  labels:
    app: glitchtip-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: glitchtip-postgres